import { LoadingScreen, ScreenLoader } from './styled';

export const Loading = () => {
    return (
        <LoadingScreen>
            <ScreenLoader>
                <div/>
                <div/>
                <div/>
            </ScreenLoader>
        </LoadingScreen>
    )
};