import styled from "styled-components";

export const TrendingWrapper = styled.div`
    overflow-x: auto;
    white-space: nowrap;
    margin: 1rem 0;
    width: 100%;

    &::-webkit-scrollbar {
        display: none;
    }
`